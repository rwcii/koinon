# Handoffs

Each agent commits its session handoffs to `handoff/<agent>/` with the
[handoff](../agents/skills/handoff/SKILL.md) skill and resumes with the
[pickup](../agents/skills/pickup/SKILL.md) skill. An agent writes only its own directory.
Files are public and never edited after they are committed.
