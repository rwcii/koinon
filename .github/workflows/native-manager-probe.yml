name: Native manager interface evidence
on:
  pull_request:
    branches: [develop]
    # Agent-process files are not installed and no test reads them; see
    # tests/test_ci_scope.py.
    paths-ignore:
      - 'agents/**'
      - 'handoff/**'
      - 'docs/sprints/**'
      - '.claude/**'
      - '.agents/**'
      - '.codex/**'
  push:
    branches: [develop, main]
    paths-ignore:
      - 'agents/**'
      - 'handoff/**'
      - 'docs/sprints/**'
      - '.claude/**'
      - '.agents/**'
      - '.codex/**'
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
permissions:
  contents: read
jobs:
  launchd-interface:
    runs-on: macos-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: python scripts/probe-launchd.py --run-isolated-job --output "$RUNNER_TEMP/koinon-manager-evidence/result.json"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: launchd-interface-evidence
          path: ${{ runner.temp }}/koinon-manager-evidence/result.json
          if-no-files-found: error
